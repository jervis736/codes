/*
** EPITECH PROJECT, 2019
** match.c
** File description:
** match_task
*/

int match(char const *s1, char const *s2)
{
    int a;
    int b;
    a = 0;
    b = 0;
    
    if (str[a] = str[b]) {
        return (1);
    }

    else if (str[b] = "*"str[b]) {
        "*"str[b] = str[a];
        return (1);
    }

    else {
        return (0);
    }
}
